const arrayFor = ['uno', 'dos', 'tres', 'cuatro'];

for (let i = 0; i < arrayFor.length; i++) {
    console.log(arrayFor[i]);
};

console.log('-----------------------------------------');

for (numeros of arrayFor) {
    console.log(numeros);
};
