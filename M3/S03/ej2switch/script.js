function cambia(valor) {

  switch (valor) {
    case '1':
      console.log(`Para la opcion '${valor}' se ejecuta este codigo.`);
      break;
      
    case '2':
      console.log(`Para la opcion '${valor}' se ejecuta este codigo.`);
      break;
      
    case '3':

    console.log(`Para la opcion '${valor}' se ejecuta este codigo.`);
      break;
      
    case '4':
      console.log(`Para la opcion '${valor}' se ejecuta este codigo.`);
      break;
      
    default:
      console.log(`La opcion '${valor}' no se encuentra en la lista.`);
      break;
  }
}